# ── 빌드 / 패키지 ──────────────────────────────────────────────────────────────
node_modules/
build/
dist/
coverage/
.next/
tsconfig.tsbuildinfo

# ── Python ─────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc

# ── 환경변수 / 시크릿 ──────────────────────────────────────────────────────────
.env*
!.env.example

# ── 로그 ───────────────────────────────────────────────────────────────────────
*.log
logs/

# ── ML 모델 파일 (Colab 학습 산출물) ───────────────────────────────────────────
# eval_report.json 은 커밋 허용 (실험 결과 기록)
models/sentiment/**/*.bin
models/sentiment/**/*.safetensors
models/sentiment/**/*.pt
models/sentiment/**/*.ckpt
models/sentiment/**/pytorch_model*
models/sentiment/**/model.safetensors*
models/sentiment/**/optimizer.pt
models/sentiment/**/tokenizer.model
models/sentiment/**/sentencepiece.bpe.model
models/sentiment/**/*.tar.gz

# checkpoints/ 아래(가중치·config·tokenizer 등)는 전부 로컬 전용.
# eval_report.json 만 커밋한다. (체크포인트 보관 정책)
models/sentiment/checkpoints/**
!models/sentiment/checkpoints/**/
!models/sentiment/checkpoints/**/eval_report.json

# ── 툴 / IDE ───────────────────────────────────────────────────────────────────
.DS_Store
.vercel
.ref/
.superpowers/
.codex/
.claude/

# ── 로컬 디자인 샘플 ──────────────────────────────────────────────────────────
public/hybrid-dashboard-sample.html

# ── 커밋 제외 스크립트 / SQL (민감 또는 일회성) ────────────────────────────────
scripts/migrate-supabase.mjs
db/migrations/000_cleanup_public_news_tables_if_empty.sql
db/migrations/001_create_target_schema.sql
